# Interview Copilot — core runtime (NO Microsoft C++ Build Tools required)
#
# Strongly recommended: Python 3.11 or 3.12 64-bit
# Python 3.14 works with this slim set, but many optional wheels are missing.
#
# Install:
#   python -m pip install -r requirements.txt

PyQt6>=6.6.0,<7
numpy>=1.26.0,<3
Pillow>=10.0.0,<12
mss>=9.0.1
# dxcam has Windows wheels; skip quietly if unavailable
dxcam>=0.0.5; platform_system=="Windows"
pytesseract>=0.3.10
sounddevice>=0.4.6
# Windows WASAPI loopback (sounddevice WasapiSettings has NO loopback= kwarg)
PyAudioWPatch>=0.2.12.6; platform_system=="Windows"
soundcard>=0.4.2; platform_system=="Windows"
groq>=0.9.0
google-genai>=0.3.0
pypdf>=4.0.0
python-docx>=1.1.0
cryptography>=42.0.0
python-dotenv>=1.0.0
keyboard>=0.13.5
httpx>=0.27.0

# Packaging (optional for day-to-day runs)
pyinstaller>=6.3.0
